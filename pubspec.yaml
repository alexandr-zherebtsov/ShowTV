name: show_tv
description: ShowTV app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Core
  get: ^4.1.4
  dio: ^4.0.0
  pretty_dio_logger: ^1.1.1

  # UI
  fluttertoast: ^8.0.6
  cupertino_icons: ^1.0.2
  flutter_rating_bar: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
